import { expect } from "chai";
import { walkingRange } from "../src/walkingRange";

describe("walking range", () => {
    it("should handle first example", () => {
        expect(walkingRange()).to.equal(0);
    }
    )
})